// 1. Write a JavaScript program that displays the largest integer among two integers.
let a = Number(prompt("Enter Fist Number", 0));
let b = Number(prompt("Enter Second Number", 0));
if (a > b) {
  document.write(`Largest Number ${a}`);
} else {
  document.write(`Largest Number ${b}`);
}
